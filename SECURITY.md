# 安全政策

## 支持范围

当前项目处于 `0.x` 黑客松原型阶段，仅维护最新提交。它不是安全关键、医疗或生产监控系统，不应直接暴露到公网。

## 报告漏洞

请不要为安全漏洞创建公开 Issue，也不要附加真实配对码、会话图片、音频或可识别个人身份的数据。

仓库发布后，请使用 GitHub 的 **Security → Report a vulnerability** 私密报告功能。报告应包含：

- 受影响版本或提交；
- 最小复现步骤；
- 可能影响；
- 已完成匿名化的日志或样例；
- 建议修复（如有）。

维护者会尽快确认报告、评估影响，并在修复可用后协调披露。原型项目无法承诺固定响应 SLA。

## 部署建议

- 仅在可信局域网开放 TCP `8780`、TCP `8766` 和 UDP `37021`。
- ASR、TTS、Ollama 与 vLLM 默认只监听 `127.0.0.1`。
- 配对码仅通过进程环境临时注入，不写入 `.env`、日志或命令参数。
- 定期清理 `runtime/focus/`，并限制会话图片、音频和报告目录的访问权限。
- 不要使用真实用户数据提交测试、Issue 或 Pull Request。
