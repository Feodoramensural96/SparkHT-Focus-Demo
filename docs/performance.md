# 性能记录

所有数字均来自本机真实 HTTP 请求。未完成的真机或 Step3 项目保持“待实测”，不使用理论吞吐代替。

| 项目 | 实测 | 验收线 | 状态/说明 |
|---|---:|---:|---|
| Qwen ASR 8010，0.5 秒静音 WAV | 10 轮 P50 0.770 s / P95 0.920 s | 快链路组成项 | 冷态曾为 2.073 s；静音仅为协议/加载探测，不代表中文语音准确率 |
| Ollama 0.6B 冷加载 | 4.916–6.301 s | — | 需要启动预热 |
| Ollama 0.6B 预热后 | 10 轮 P50 0.162 s / P95 0.193 s | 快链路组成项 | 冷态曾为 4.916–6.301 s |
| Qwen3-TTS 8030 首个 PCM 音频块 | 10 轮 P50 0.687 s / P95 0.710 s | 快链路组成项 | 不能用 0.003 s 的 HTTP 响应头时间代替首音 |
| Qwen3-TTS 8030 完整响应 | 10 轮 P50 0.861 s / P95 1.879 s | — | 音频长度随生成结果变化，不影响首块统计 |
| 暖态快链路组成项 P50/P95 之和 | 1.619 s / 1.823 s | 为端到端 ≤ 4 s 留出余量 | 尚不含 VAD、网络调度和机器人开始播放；不是端到端指标 |
| 暖机后机器人相机 P95 | 待真机实测 | ≤ 0.7 s | 需要 sdk.control.app 配对 |
| 语音结束到机器人首音 P50/P95 | 待真机实测 | ≤ 3/4 s | 需要真实麦克风与播放链路 |
| Step3 单图 640×480、64 tokens | 1 轮 2.829 s | — | FP8、真实 8040 HTTP、JSON Schema 输出；合成测试图，不冒充机器人抓拍 |
| Step3 四图 640×480、192 tokens | 3 轮 P50 12.707 s / P95 14.765 s | 目标 ≤ 20 s，硬上限 30 s | 3/3 成功；单并发；与快链路压力同时运行 |
| 视觉负载期间 Qwen ASR 8010 | 3 轮 P50 1.735 s / P95 1.781 s | 快链路组成项 | 0.5 秒静音 WAV，仅验证资源共存 |
| 视觉负载期间 TTS 首 PCM | 3 轮 P50 1.855 s / P95 1.962 s | 快链路组成项 | 与四图 Step3 同时运行 |
| 视觉负载期间 Ollama | 3 轮 P50 0.260 s / P95 4.912 s | 快链路组成项 | 首轮出现冷峰；真机路径必须先取消 Step3，不能用硬并发值冒充首音 |
| 90 秒有效抓拍/成功批次 | 待真机实测 | ≥ 8 帧 / ≥ 2 批 | 不伪造 |

Step3 数据使用四张 640×480 合成协议图，证明真实 FP8 服务、四图顺序、结构化输出和时延，不代表机器人场景识别准确率。服务使用无思维链模板和 vLLM `response_format=json_schema`，四图输出严格保留真实 frame ID；Pydantic 继续做应用层复核。

启动时并行预热 ASR、Ollama 和 TTS。应用层时延还需在机器人连接后测量 ASR 最终文本、首个 TTS PCM 和机器人开始播放三个时间点。
